D1= {'a': {'b': {'c': 'd'}}}
def iterdict(d):
  for k,v in d.items():        
     if isinstance(v, dict):
         iterdict(v)
     else:      
         key = input("What key would you like to find?\n")
         if key == k :
            print (k,":",v)
         else :
             print ("nothing")
iterdict(D1)
